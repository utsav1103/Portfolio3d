/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'github-readme-streak-stats.vercel.app',
            'github-readme-stats.vercel.app',
            'skillicons.dev'
        ],
    },
};

export default nextConfig;
